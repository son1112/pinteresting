
<div style="margin-top: 10em;">

  <div class="row">
    <div class="col-md-offset-2 col-md-8">
      <div class="panel panel-default">
	<div class="panel-heading center">
	  <%= image_tag @pin.image.url(:medium) if @pin.image.url != "/images/original/missing.png" %>
	</div>
	<div class="panel-body">
	  <p>
	    <strong><%= @pin.user.email[/^\w+/] if @pin.user %></strong>
	  </p>

	  <p>
	    <%= @pin.description %>
	  </p>

	  <% if @pin.user == current_user %>
	    <%= link_to 'Edit', edit_pin_path(@pin), class: "btn btn-primary" %>
	  <% end %>
	  <%= link_to 'Pins', pins_path, class: "btn btn-primary" %>
	  <% if user_signed_in? %>
	    <%= link_to 'New Pin', new_pin_path, class: "btn btn-primary" %>
	  <% end %>
	</div>
      </div>
    </div>
  </div>
</div>
